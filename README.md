# 🧩 CRUD com MySQL Workbench em Python

Este projeto demonstra a implementação de um **CRUD (Create, Read, Update, Delete)** utilizando **Python** e **MySQL**.  
O script estabelece uma conexão com o banco de dados através do **MySQL Connector**, realizando operações na tabela `Tb_funcionario`.

---

## 📘 Funcionalidades

O código realiza as seguintes operações:

- **Conectar** ao banco de dados MySQL (`create_connection`)
- **Criar** novos registros de funcionários (`incluir_funcionario`)
- **Consultar** os funcionários existentes (`consultar_funcionarios`)
- **Atualizar** informações de um funcionário (`alterar_funcionario`)
- **Excluir** funcionários (`excluir_funcionario`)

---

## ⚙️ Requisitos

Antes de rodar o projeto, certifique-se de ter instalado:

- [Python 3.8+](https://www.python.org/downloads/)
- [MySQL](https://dev.mysql.com/downloads/)
- Biblioteca `mysql-connector-python`

- # 🧩 CRUD com MySQL Workbench em Python

Este projeto demonstra a implementação de um **CRUD (Create, Read, Update, Delete)** utilizando **Python** e **MySQL**.  
O script estabelece uma conexão com o banco de dados através do **MySQL Connector**, realizando operações na tabela `Tb_funcionario`.

---

## 📘 Funcionalidades

O código realiza as seguintes operações:

- **Conectar** ao banco de dados MySQL (`create_connection`)
- **Criar** novos registros de funcionários (`incluir_funcionario`)
- **Consultar** os funcionários existentes (`consultar_funcionarios`)
- **Atualizar** informações de um funcionário (`alterar_funcionario`)
- **Excluir** funcionários (`excluir_funcionario`)

---

## ⚙️ Requisitos

Antes de rodar o projeto, certifique-se de ter instalado:

- [Python 3.8+](https://www.python.org/downloads/)
- [MySQL](https://dev.mysql.com/downloads/)
- Biblioteca `mysql-connector-python`

Para instalar o conector MySQL, use o comando:

```bash
pip install mysql-connector-python
